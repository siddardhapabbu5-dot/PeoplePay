{
  "name": "peoplepay",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:db": "docker compose up -d",
    "setup": "docker compose up -d && npm install && npm run prisma:generate -w server && npm run prisma:migrate -w server && npm run seed -w server",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
